description: >
  Example usage of the ZeroThreat AI for vulnerability scanning.

usage:
  version: 2.1
  orbs:
    vuln-scan: zerothreat-ai/dast-scanner@0.0.2
  workflows:
    zerothreat-security-scan:
      jobs:
        - vuln-scan/scan-job:
            ZT_TOKEN: "AWD4TRX" # Replace with actual token.
